#ifndef MCSL_DYN_STRING_SPAN_CPP
#define MCSL_DYN_STRING_SPAN_CPP

#include "dyn_str_span.hpp"

// //!constructor from c-dyn_str_span and length
// constexpr mcsl::dyn_str_span::dyn_str_span(str_base& str):
// _ptrToBuf(str.ptr_to_buf()),_begin(0),_size(str.size()) {
   
// }
// //!constructor from c-dyn_str_span and length
// constexpr mcsl::dyn_str_span::dyn_str_span(str_base& str, const uint size):
// _ptrToBuf(str.ptr_to_buf()),_begin(0),_size(size) {
//    assert(size <= str.size());
// }
// //!constructor from c-dyn_str_span and length
// constexpr mcsl::dyn_str_span::dyn_str_span(str_base& str, const uint begin, const uint size):
// _ptrToBuf(str.ptr_to_buf()),_begin(begin),_size(size) {
//    assert((begin + size) <= str.size());
// }

#endif //MCSL_DYN_STRING_SPAN_CPP