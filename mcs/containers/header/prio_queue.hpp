// #pragma once
// #ifndef PRIO_QUEUE_HPP
// #define PRIO_QUEUE_HPP

// #include "MCS.hpp"

// template<typename T, cmpfunc(T)> class mcs::prio_queue {
//    private:
//       node<T> _front;
//       node<T> _back;
//    public:

// };

// #endif //PRIO_QUEUE_HPP